/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: ce fichier est normalement généré automatiquement par `next dev`
// ou `next build`. Il est inclus ici pour que le tout premier build
// (avant qu'il n'ait eu la chance de le générer) ne plante pas.
